server:
  port: 8080

spring:
  application:
    name: smart-meeting-room

  datasource:
    url: jdbc:mysql://localhost:3306/meeting_room_system
    username: root
    password: Santosh@1497
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
