server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: always

spring:
  application:
    name: smart-meeting-room

  datasource:
    url: jdbc:mysql://localhost:3306/meeting_room_system?useSSL=false&serverTimezone=UTC
    username: root
    password: Santosh@1497
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: my-super-secret-jwt-key-for-smart-meeting-room-2025
  expiration: 600000

logging:
  level:
    org.hibernate.SQL: debug
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    com.project.smartmeetingroom: DEBUG
